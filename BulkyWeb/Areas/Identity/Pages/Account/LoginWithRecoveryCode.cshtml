@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "恢復代碼認證 Recovery code verification";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
    您已請求使用恢復代碼登入。系統不會記住此登入信息，除非您提供
    登入時輸入身份驗證器應用程式代碼或停用 2FA 並再次登入。
    You have requested to log in with a recovery code. This login will not be remembered until you provide
    an authenticator app code at log in or disable 2FA and log in again.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="恢復代碼 RecoveryCode" />
                <label asp-for="Input.RecoveryCode" class="form-label"></label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">登入</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}