@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "配置身份驗證器應用 Configure authenticator app";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div>
    <p>若要使用身份驗證器應用，請執行下列步驟:</p>
    <ol class="list">
        <li>
            <p>
                下載雙重驗證應用程式（例如 Microsoft Authenticator）
                <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a> and
                <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a> or
                Google 驗證器
                <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=en">Android</a> and
                <a href="https://itunes.apple.com/us/app/google-authenticator/id388497605?mt=8">iOS</a>.
            </p>
        </li>
        <li>
            <p>掃描二維碼或輸入此金鑰 <kbd>@Model.SharedKey</kbd> 進入您的雙重認證器應用程式。空格和大小寫無關緊要。.</p>
            <div class="alert alert-info">了解如何 <a href="https://go.microsoft.com/fwlink/?Linkid=852423">啟用二維碼生成</a>.</div>
            <div id="qrCode"></div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>
        <li>
            <p>
                掃描二維碼或輸入上述金鑰後，雙重認證應用程式將為您提供
                具有唯一代碼。在下面的確認框中輸入代碼。
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="請輸入代碼。" />
                            <label asp-for="Input.Code" class="control-label form-label">驗證碼</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="w-100 btn btn-lg btn-primary">驗證</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
